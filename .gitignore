# Artefactos de empaquetado para Apps Script (regenerables desde el código)
*.zip

# Skills instaladas por el CLI externo "skills" (código de terceros; no forma
# parte del proyecto de inventarios). Se mantienen fuera del repo a propósito.
.agents/
skills-lock.json

# Binding local de clasp (cada carpeta apunta a su Apps Script: real o copia)
.clasp.json
